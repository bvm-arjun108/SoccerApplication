
spring:
  application:
    name: teamsapplication

  datasource:
    url: jdbc:postgresql://localhost:5433/soccerdb
    username: postgres
    password: Ganapati108!
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  config:
    import: optional:configserver:http://localhost:9097
  profiles:
    active: dev

server:
  port: 9091



